import styled from 'styled-components';

export const PageHeaderContainer = styled.div`
  h2 {
    position: relative;
    z-index: 9;
  }

  svg {
    position: absolute;
    z-index: 0;
    top: 0;
    right: 0;
  }
`;
